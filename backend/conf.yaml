
# refis conf
redis:
  host: localhost
  port: 6379
  db: 0


crypto:
  secret: test
  # mode: AES_GCM
  mode: FERNET

flask:
# this way flask will serve the front end
  serve_front: false
  cors:
    - http://localhost:3000
    - localhost:3000